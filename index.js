const somaHorasExtras = (salario, valorHoraExtra) => salario + valorHoraExtra;

const calculaDescontos = (salario, deducoes) => salario - deducoes;

const multiplicaDepoisDobra = (valor1, valor2) => (valor1 * valor2) * 2;

export { somaHorasExtras, calculaDescontos, multiplicaDepoisDobra };
